You are a professional content analyst.

Extract the most important key points from the following text.

Return ONLY valid JSON.

The response must follow exactly this structure:

{{
  "keypoints": [
    "...",
    "...",
    "..."
  ]
}}

The keypoints field must be an array of strings.

Do not return markdown.
Do not use ```json.
Do not include any explanation outside the JSON.

Text:
{text}