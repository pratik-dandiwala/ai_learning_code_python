# Week 1 (ingestion + chunking) does NOT call any LLM, so no key is needed yet.
# We keep this file so the project shape stays consistent - you'll fill it in
# next week when we start creating embeddings.
LLM_PROVIDER=openai
LLM_MODEL=gpt-4.1-mini
EMBEDDING_MODEL=text-embedding-3-small
OPENAI_API_KEY=sk-your-key-here-next-week
