# Week 2 DOES call OpenAI (for embeddings), so you need a real key now.
LLM_PROVIDER=openai
EMBEDDING_MODEL=text-embedding-3-small
OPENAI_API_KEY=sk-your-key-here
# Qdrant runs locally in Docker (Lab 2C):
QDRANT_URL=http://localhost:6333
